export enum ButtonType {
  SUBMIT = 'submit',
  BUTTON = 'button',
  RESET = 'reset',
}

export enum ButtonVariant {
  PRIMARY = 'primary',
  SECONDARY = 'secondary',
  DANGER = 'danger',
}

export enum ComponentState {
  ACTIVE = 'active',
  DISABLED = 'disabled',
  LOADING = 'loading',
  ERROR = 'error',
}